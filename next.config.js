/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['cdn.sanity.io', 'images.unsplash.com','img.freepik.com','plainenglish.com']
    }
}

module.exports = nextConfig
